  on: [workflow_dispatch]
  
  jobs:
    checkout:
      runs-on: ubuntu-latest
      steps:
      - uses: actions/checkout@v2
      - uses: ./composite
      - uses: ./composite
        with:
          path: some-subdirectory
