  on: [workflow_dispatch]
  
  jobs:
    checkout:
      runs-on: ubuntu-latest
      steps:
      - uses: actions/checkout@v2
      - uses: ./composite@main
      - uses: ./composite@main
        with:
          path: some-subdirectory
